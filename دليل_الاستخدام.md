# دليل تشغيل برنامج حجز كرامة

## ماذا يفعل البرنامج؟

1. يراقب مجلد `register_cases` بحثاً عن ملفات `.xml` أو `.txt` (كل ملف = حالة واحدة).
2. يفتح موقع الكشف الطبي ويسجل البيانات تلقائياً.
3. عند النجاح: ينقل الملف إلى `register_finished`، يسجل الرقم القومي في `processed_state.json` (لتجنب التكرار)، ويرسل إشعاراً على قناة تيليجرام.

---

## التثبيت لأول مرة (على جهاز العميل)

### المتطلبات

- Windows 10 أو أحدث
- Python 3.10 أو أحدث ([python.org](https://www.python.org/downloads/))
  - عند التثبيت: فعّل **Add Python to PATH**

### خطوات التثبيت

1. انسخ مجلد البرنامج كاملاً إلى الجهاز (مثلاً `C:\KaramaBot`).
2. شغّل **`INSTALL.bat`** (نقرة مزدوجة) وانتظر حتى ينتهي.
3. افتح **`register.ini`** بمحرر نصوص وعدّل:
   - `TELEGRAM_BOT` = توكن البوت من BotFather
   - `TELEGRAM_CHANNEL` = معرف القناة (`@اسم_القناة` أو `-100...`)
   - `THREADS_COUNT` = عدد الحالات المتزامنة (مثلاً 4)
   - `SLEEP` = ثوانٍ بين كل دورة فحص للمجلد (مثلاً 10)
4. شغّل **`TEST_TELEGRAM.bat`** — يجب أن تظهر رسالة تجريبية في القناة.
5. ضع ملف حالة تجريبي في `register_cases` ثم شغّل **`START.bat`**.

---

## الاستخدام اليومي

1. ضع ملفات الحالات في مجلد **`register_cases`**.
2. شغّل **`START.bat`** واترك النافذة مفتوحة.
3. راقب القناة على تيليجرام للإشعارات.
4. الملفات الناجحة تنتقل تلقائياً إلى **`register_finished`**.
5. لإيقاف البرنامج: اضغط **Ctrl+C** في نافذة التشغيل.

---

## شكل ملف الحالة (XML)

اسم الملف يُفضّل أن يكون الرقم القومي، مثال: `23703142200169.xml`

```xml
<case>
    <ssn>23703142200169</ssn>
    <firstName>سمر</firstName>
    <secondName>حسين</secondName>
    <thirdName>زينهم</thirdName>
    <fourthName>عبدالعزيز</fourthName>
    <city>مدينة ناصر</city>
    <address>مدينة ناصر</address>
    <phoneNumber>01552166635</phoneNumber>
    <governorate>
        <option checked="1" value="22">بني سويف</option>
    </governorate>
    <MaritalStatus>
        <option checked="1" value="1">متزوج</option>
    </MaritalStatus>
    <Job>
        <option checked="1" value="56">بدون عمل</option>
    </Job>
    <ddl>
        <option checked="1" value="3">أمراض مزمنة</option>
    </ddl>
    <note>ملاحظة</note>
</case>
```

### قيم نوع الإعاقة (`ddl` → value)

| value | النوع |
|------|--------|
| 1 | إعاقة حركية |
| 2 | إعاقة ذهنية |
| 7 | طيف توحد |
| 3 | أمراض مزمنة |
| 4 | إعاقة سمعية |
| 5 | إعاقة بصرية |

---

## إعداد تيليجرام (ملخص)

1. أنشئ بوت عبر **@BotFather** → `/newbot` → انسخ التوكن إلى `TELEGRAM_BOT`.
2. أضف البوت **مشرفاً** على القناة مع صلاحية **نشر الرسائل**.
3. للقناة العامة: `TELEGRAM_CHANNEL = @اسم_القناة`
4. للقناة الخاصة: استخدم الرقم `-100...` من @RawDataBot بعد إعادة توجيه رسالة من القناة.

---

## ملفات مهمة

| الملف / المجلد | الوظيفة |
|----------------|---------|
| `register.ini` | الإعدادات |
| `register_cases` | ملفات الحالات الجديدة |
| `register_finished` | الحالات المنتهية بنجاح |
| `processed_state.json` | سجل الأرقام القومية المسجلة (لا تحذفه إلا عمداً) |
| `automation.log` | سجل التشغيل للدعم الفني |

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---------|-----|
| لا تصل رسائل تيليجرام | شغّل `TEST_TELEGRAM.bat` — تأكد من التوكن والقناة والبوت كمشرف |
| الملف لا يُعالج | تأكد من وجود `<ssn>` وامتداد `.xml` أو `.txt` |
| نفس الملف يُتخطى | الرقم مسجل مسبقاً في `processed_state.json` |
| خطأ Playwright | أعد تشغيل `INSTALL.bat` |
| فشل التسجيل على الموقع | راجع `automation.log` وصورة `error_SSN_*.png` إن وُجدت |

---

## تسليم البرنامج للعميل

انسخ المجلد التالي **بدون** ملفات التطوير الاختيارية:

**يُسلّم:**
- `main.py`, `test_telegram.py`, `requirements.txt`, `register.ini`
- `INSTALL.bat`, `START.bat`, `TEST_TELEGRAM.bat`
- `دليل_الاستخدام.md`, `sample_case.txt`
- مجلدات فارغة: `register_cases`, `register_finished`

**لا يُسلّم (اختياري للمطور فقط):**
- `probe_form.py`, `form_elements.txt`, `page_source.html`

**قبل التسليم:** ضع إعدادات تيليجرام الصحيحة في `register.ini` أو اترك قالباً واشرح للعميل خطوة التعديل.

**أمان:** لا تشارك توكن البوت علناً. إذا تسرّب، أعد إنشاءه من BotFather عبر `/revoke`.
